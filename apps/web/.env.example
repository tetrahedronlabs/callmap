# Public browser token used by the map component.
VITE_MAPBOX_ACCESS_TOKEN=pk.example

# Optional direct PostgreSQL URL for Drizzle Studio or live introspection.
# Schema generation and checks do not require it. Never commit a real value.
DATABASE_URL=postgresql://user:password@host:5432/postgres?sslmode=require
